This is the example post. Delete this file, delete its entry in `content.js`,
and write your own — or just type over this text.

Everything below is only here to show what formatting is available.

## A heading

Regular paragraphs need no special treatment. **Bold** and *italic* work as
you'd expect, and so do [links](https://example.com).

- A bullet
- Another bullet

1. A numbered point
2. And the next

> A quote, for when someone else said it better.

`Inline code` looks like this, and a block looks like this:

```js
console.log("hello");
```

---

That's the lot. Write in plain text and it will look right.
